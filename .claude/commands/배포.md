변경된 파일을 확인하고 git add → commit → push 까지 배포해줘.

1. `git status`로 변경 파일 확인
2. `git diff --staged` 및 `git diff`로 변경 내용 파악
3. 변경 내용을 요약한 한국어 커밋 메시지 작성 (50자 이내 제목)
4. `git add` → `git commit` → `git push` 순서로 실행
5. 완료 후 push된 브랜치와 커밋 해시 알려줘
